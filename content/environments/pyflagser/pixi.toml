# pyflagser — Python bindings to flagser (directed flag-complex persistence; the substrate the
# Persistent Directed Flag Laplacian extends). Wheel-only on PyPI; recipe sources a GitHub tag.
# Compiled → DRAFT recipe. In-repo recipe → L1. (v0.4.7)
[workspace]
preview = ["pixi-build"]
channels = ["conda-forge", "bioconda"]
platforms = ["linux-64"]

[dependencies]
pyflagser = { path = "../../../recipes/pyflagser" }

